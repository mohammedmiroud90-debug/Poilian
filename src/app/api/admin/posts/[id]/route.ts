import { NextResponse } from "next/server";
import { currentAdmin, headers, parseConfigured, url } from "@/lib/admin";
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await currentAdmin()) return NextResponse.json({ error: "Please sign in again." }, { status: 401 }); if (!parseConfigured) return NextResponse.json({ error: "Parse is not configured." }, { status: 503 }); const { id } = await params; const className = new URL(request.url).searchParams.get("class") === "BlogPost" ? "BlogPost" : "Article"; const response = await fetch(`${url}/classes/${className}/${encodeURIComponent(id)}`, { method: "DELETE", headers }); return response.ok ? NextResponse.json({ ok: true }) : NextResponse.json({ error: "Post could not be deleted." }, { status: 500 }); }

import { NextResponse } from "next/server";
import { currentAdmin, headers, parseConfigured, url } from "@/lib/admin";
const slugValid = (value: unknown) => typeof value === "string" && /^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(value);
export async function POST(request: Request) { if (!await currentAdmin()) return NextResponse.json({ error: "Please sign in again." }, { status: 401 }); if (!parseConfigured) return NextResponse.json({ error: "Parse is not configured." }, { status: 503 }); const body = await request.json() as Record<string, unknown>; if (typeof body.title !== "string" || !body.title.trim() || !slugValid(body.slug)) return NextResponse.json({ error: "Add a title and a valid lowercase URL slug." }, { status: 400 }); const payload = { title: body.title.trim(), slug: body.slug, excerpt: typeof body.excerpt === "string" ? body.excerpt : "", content: typeof body.content === "string" ? body.content : "", category: typeof body.category === "string" && body.category.trim() ? body.category : "Personal notes", author: typeof body.author === "string" && body.author.trim() ? body.author : "Belhachemia Mohammed", status: body.status === "draft" ? "draft" : "published", publishedAt: new Date().toISOString() }; const className = body.className === "BlogPost" ? "BlogPost" : "Article"; const id = typeof body.id === "string" && body.id !== "welcome" ? body.id : ""; const response = await fetch(id ? `${url}/classes/${className}/${encodeURIComponent(id)}` : `${url}/classes/${className}`, { method: id ? "PUT" : "POST", headers, body: JSON.stringify(payload) }); if (!response.ok) return NextResponse.json({ error: "Post could not be saved. Check your Parse permissions." }, { status: 500 }); return NextResponse.json({ ok: true, id: id || (await response.json() as { objectId: string }).objectId, className }); }

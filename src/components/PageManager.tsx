"use client";
import { KeyboardEvent, useEffect, useRef, useState } from "react";
import type { SitePage } from "@/lib/pages";

type Draft = Omit<SitePage, "id" | "updatedAt"> & { id?: string };
const blank = (): Draft => ({ title: "", slug: "", excerpt: "", content: "<p>Start writing your page here…</p>", status: "published", showInNavigation: true, navigationLabel: "" });
const words = (html: string) => html.replace(/<[^>]*>/g, " ").trim().split(/\s+/).filter(Boolean).length;

export function PageManager({ initialPages }: { initialPages: SitePage[] }) {
  const [pages, setPages] = useState(initialPages); const [draft, setDraft] = useState<Draft | null>(null); const [notice, setNotice] = useState(""); const [format, setFormat] = useState("Paragraph"); const editor = useRef<HTMLDivElement>(null);
  function edit(page: SitePage) { setNotice(""); setDraft(page); requestAnimationFrame(() => { if (editor.current) editor.current.innerHTML = page.content; }); }
  function startNew() { setNotice(""); const next = blank(); setDraft(next); requestAnimationFrame(() => { if (editor.current) editor.current.innerHTML = next.content; }); }
  function update(field: keyof Draft, value: string | boolean) { if (!draft) return; setDraft({ ...draft, [field]: value }); }
  function sync() { if (!editor.current) return; update("content", editor.current.innerHTML); const node = document.queryCommandValue("formatBlock") || "p"; setFormat(String(node).replace(/[<>]/g, "").toLowerCase() === "p" ? "Paragraph" : String(node).replace(/[<>]/g, "").toUpperCase()); }
  function command(action: string, value?: string) { editor.current?.focus(); document.execCommand(action, false, value); sync(); }
  function addLink() { const href = prompt("Paste the link URL"); if (href) command("createLink", href); }
  function addImage() { const url = prompt("Paste an image URL"); if (url) command("insertHTML", `<img src="${url.replace(/"/g, "&quot;")}" alt="" />`); }
  function addTable() { command("insertHTML", "<table><thead><tr><th>Heading</th><th>Heading</th></tr></thead><tbody><tr><td>Text</td><td>Text</td></tr><tr><td>Text</td><td>Text</td></tr></tbody></table><p><br></p>"); }
  async function save() { if (!draft || !editor.current) return; const value = { ...draft, content: editor.current.innerHTML, navigationLabel: draft.navigationLabel || draft.title }; setNotice("Saving…"); const response = await fetch("/api/admin/pages", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(value) }); const result = await response.json(); if (!response.ok) return setNotice(result.error ?? "Could not save this page."); const updated = { ...value, id: draft.id ?? result.id } as SitePage; setPages((list) => list.some((page) => page.id === updated.id) ? list.map((page) => page.id === updated.id ? updated : page) : [...list, updated]); setDraft(updated); setNotice("Saved. Your public page and navigation are updated."); }
  async function remove(page: SitePage) { if (!confirm(`Delete “${page.title}”?`)) return; const response = await fetch(`/api/admin/pages/${page.id}`, { method: "DELETE" }); if (response.ok) { setPages((list) => list.filter((item) => item.id !== page.id)); setDraft(null); setNotice("Page deleted."); } else setNotice("Could not delete this page."); }
  function keyDown(event: KeyboardEvent<HTMLDivElement>) { if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "s") { event.preventDefault(); void save(); } }
  useEffect(() => { const saveShortcut = (event: globalThis.KeyboardEvent) => { if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "s") { event.preventDefault(); void save(); } }; window.addEventListener("keydown", saveShortcut); return () => window.removeEventListener("keydown", saveShortcut); });
  return <main className="admin-shell"><header className="admin-header"><div><p className="section-label">POILIAN CMS</p><h1>Pages</h1></div></header><div className="admin-layout"><aside><button className="new-page" onClick={startNew}>+ New page</button><div className="page-list">{pages.map((page) => <div className={draft?.id === page.id ? "selected" : ""} key={page.id}><button onClick={() => edit(page)}><strong>{page.title}</strong><span>/{page.slug} · {page.status}</span></button><button className="delete-button" aria-label={`Delete ${page.title}`} onClick={() => remove(page)}>×</button></div>)}</div></aside>{draft ? <section className="page-editor"><div className="editor-title"><div><p className="section-label">{draft.id ? "EDIT PAGE" : "NEW PAGE"}</p><h2>{draft.title || "Untitled page"}</h2></div>{draft.slug && <a href={`/pages/${draft.slug}`} target="_blank">View page ↗</a>}</div><div className="field-grid"><label>Page title<input value={draft.title} onChange={(e) => { update("title", e.target.value); if (!draft.slug) update("slug", e.target.value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "")); }} placeholder="Projects" /></label><label>URL slug<input value={draft.slug} onChange={(e) => update("slug", e.target.value.toLowerCase().replace(/\s+/g, "-"))} placeholder="projects" /></label></div><label>Short introduction<textarea value={draft.excerpt} onChange={(e) => update("excerpt", e.target.value)} placeholder="A short line shown below the title." /></label><div className="editor-card"><div className="editor-card-head"><strong>Page content</strong><span>Rich text editor</span></div><div className="so-toolbar" role="toolbar" aria-label="Page formatting"><select aria-label="Text style" value={format} onChange={(e) => command("formatBlock", e.target.value === "Paragraph" ? "p" : e.target.value.toLowerCase())}><option>Paragraph</option><option>H2</option><option>H3</option></select><i /><button title="Bold" onClick={() => command("bold")}><b>B</b></button><button title="Italic" onClick={() => command("italic")}><i>I</i></button><button title="Underline" onClick={() => command("underline")}><u>U</u></button><i /><button title="Numbered list" onClick={() => command("insertOrderedList")}>1.</button><button title="Bulleted list" onClick={() => command("insertUnorderedList")}>☷</button><button title="Quote" onClick={() => command("formatBlock", "blockquote")}>❝</button><button title="Code block" onClick={() => command("formatBlock", "pre")}>{"</>"}</button><i /><button title="Add link" onClick={addLink}>↗</button><button title="Add image" onClick={addImage}>▧</button><button title="Add table" onClick={addTable}>▦</button><button title="Horizontal rule" onClick={() => command("insertHorizontalRule")}>—</button><i /><button title="Undo" onClick={() => command("undo")}>↶</button><button title="Redo" onClick={() => command("redo")}>↷</button><button title="Remove formatting" onClick={() => command("removeFormat")}>Tx</button></div><div className="rich-editor stack-editor" ref={editor} contentEditable suppressContentEditableWarning data-placeholder="Write your page content here…" onInput={sync} onKeyUp={sync} onMouseUp={sync} onKeyDown={keyDown} onPaste={(event) => { event.preventDefault(); document.execCommand("insertText", false, event.clipboardData.getData("text/plain").replace(/\r\n?/g, "\n")); sync(); }} /><footer className="editor-status"><span>{words(draft.content)} words · {Math.max(1, Math.ceil(words(draft.content) / 220))} min read</span><span>Use Ctrl/Cmd + S to save</span></footer></div><div className="editor-options"><label><input type="checkbox" checked={draft.showInNavigation} onChange={(e) => update("showInNavigation", e.target.checked)} /> Show in header navigation</label><label>Navigation label<input value={draft.navigationLabel} onChange={(e) => update("navigationLabel", e.target.value)} placeholder={draft.title || "Projects"} /></label><label>Status<select value={draft.status} onChange={(e) => update("status", e.target.value)}><option value="published">Published</option><option value="draft">Draft</option></select></label></div><footer className="save-bar"><span>{notice}</span><button onClick={() => void save()}>Save page</button></footer></section> : <section className="empty-editor"><h2>Create your first dynamic page</h2><p>Select a page to edit it or use “New page” to add Projects, Founder, or anything else.</p></section>}</div></main>;
}
